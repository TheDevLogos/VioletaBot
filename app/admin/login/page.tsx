export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="login"><div className="brand">VioletaBot</div><h1>Acceso institucional</h1><p>Panel reservado para personal autorizado.</p>{q.error&&<p className="bubble panic">Correo o contraseña incorrectos.</p>}<form action="/api/auth/login" method="post"><label>Correo institucional</label><input className="input" name="email" type="email" required autoComplete="email" placeholder="operadora@municipio.gob.mx"/><label>Contraseña</label><input className="input" name="password" type="password" required autoComplete="current-password"/><button className="btn primary" style={{width:'100%'}}>Ingresar</button></form></main>}

'use client';
import { useState } from 'react';export function OrganizationCreateForm(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/organizations/create',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await r.json();setMsg(r.ok?'Entidad creada. Recarga la página para verla.':j.error||'No se pudo crear.');if(r.ok)e.currentTarget.reset()}return <form onSubmit={submit} className="opFormGrid"><div><label className="opLabel">Nombre de entidad</label><input className="opInput" name="name" required/></div><div><label className="opLabel">Tipo</label><select className="opSelect" name="organization_type"><option value="municipal_womens_institute">Instituto Municipal de las Mujeres</option><option value="municipality">Municipio</option><option value="public_safety">Seguridad Pública</option><option value="state_agency">Dependencia estatal</option><option value="other">Otra</option></select></div><div className="full"><button className="opBtn secondary">Crear entidad</button>{msg&&<span className="opMuted" style={{marginLeft:10}}>{msg}</span>}</div></form>}

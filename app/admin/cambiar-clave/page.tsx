import { requireStaffPage } from '@/lib/auth/staff';export default async function ChangePassword(){await requireStaffPage(undefined,true);return <main className="opShell"><div className="opPassword"><div className="opBrand">VioletaBot</div><h1>Cambia tu contraseña</h1><p className="opMuted">Esta cuenta fue creada con una contraseña temporal. Define una contraseña privada de al menos 12 caracteres para continuar.</p><form action="/api/auth/change-password" method="post"><label className="opLabel">Nueva contraseña</label><input className="opInput" type="password" name="password" minLength={12} required autoComplete="new-password"/><label className="opLabel" style={{marginTop:12}}>Confirmar contraseña</label><input className="opInput" type="password" name="confirm" minLength={12} required autoComplete="new-password"/><button className="opBtn" style={{marginTop:16,width:'100%'}}>Guardar y entrar al Centro</button></form></div></main>}

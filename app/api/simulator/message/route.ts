import { randomUUID } from 'node:crypto';import { NextResponse } from 'next/server';import { evaluateRisk } from '@/lib/risk/engine';import { getStaffContext } from '@/lib/auth/staff';import { supabaseAdmin } from '@/lib/supabase/admin';
function replyFor(level:string){if(level==='critical')return 'Estoy contigo. Si puedes hacerlo sin ponerte en mayor riesgo, comparte tu ubicación. Prepararé el escalamiento para atención humana conforme al protocolo.';if(level==='high')return 'Lo que describes puede ser una situación de riesgo. Quiero ayudarte a conectarte con una persona de apoyo. ¿Es seguro continuar hablando?';if(level==='medium')return 'Lo que cuentas merece atención. Si quieres, podemos revisar juntas qué está pasando y pensar en una red de apoyo segura.';return 'Estoy aquí contigo. Podemos ir paso a paso. ¿Te sientes segura en este momento?'}
export async function POST(req:Request){const ctx=await getStaffContext();if(!ctx||!ctx.active||!ctx.organizationId)return NextResponse.json({error:'No autorizado'},{status:401});if(!['super_admin','admin','supervisor','operator'].includes(ctx.role))return NextResponse.json({error:'Sin permiso para simular'},{status:403});const body=await req.json().catch(()=>({}));const text=String(body.text||'').trim();if(!text||text.length>5000)return NextResponse.json({error:'Mensaje inválido'},{status:400});const db=supabaseAdmin();let conversationId=String(body.conversationId||'');let conv:any=null;if(conversationId){const {data}=await db.from('conversations').select('id,organization_id,recurrence_count,channel').eq('id',conversationId).maybeSingle();if(data?.organization_id===ctx.organizationId&&data.channel==='simulator')conv=data}if(!conv){const now=new Date();const label=`Simulación ${now.toLocaleDateString('es-MX')} ${now.toLocaleTimeString('es-MX',{hour:'2-digit',minute:'2-digit'})}`;const created=await db.from('conversations').insert({organization_id:ctx.organizationId,wa_user_id:`SIM-${randomUUID()}`,status:'open',channel:'simulator',is_test:true,subject_label:label}).select('id,organization_id,recurrence_count,channel').single();if(created.error||!created.data)return NextResponse.json({error:'No se pudo crear la simulación'},{status:500});conv=created.data;conversationId=conv.id}await db.from('messages').insert({conversation_id:conversationId,direction:'inbound',message_type:'text',content:text,metadata:{simulator:true,created_by:ctx.userId}});const risk=evaluateRisk(text,conv.recurrence_count||0);await db.from('risk_events').insert({conversation_id:conversationId,level:risk.level,score:risk.score,triggers:risk.triggers,categories:risk.categories,source_text:text});const reply=replyFor(risk.level);await db.from('messages').insert({conversation_id:conversationId,direction:'outbound',message_type:'text',content:reply,metadata:{simulator:true,risk_level:risk.level}});if(risk.requiresHuman)await db.from('alerts').insert({conversation_id:conversationId,alert_type:'operator',status:'pending',priority:risk.level,payload:{simulation:true,risk}});await db.from('conversations').update({updated_at:new Date().toISOString()}).eq('id',conversationId);await db.from('audit_logs').insert({organization_id:ctx.organizationId,actor_id:ctx.userId,action:'simulator.message_evaluated',entity_type:'conversation',entity_id:conversationId,metadata:{risk_level:risk.level,score:risk.score}});return NextResponse.json({ok:true,conversationId,risk,reply});}

export async function sendWhatsApp(to:string,body:string){const token=process.env.WHATSAPP_ACCESS_TOKEN,id=process.env.WHATSAPP_PHONE_NUMBER_ID;if(!token||!id)throw new Error('WhatsApp not configured');const r=await fetch(`https://graph.facebook.com/v23.0/${id}/messages`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({messaging_product:'whatsapp',to,type:'text',text:{body}})});if(!r.ok)throw new Error(`WhatsApp error ${r.status}`);return r.json()}
